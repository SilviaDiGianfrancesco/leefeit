<p id="notice"><%= notice %></p>

<h1>Listing Cosmetics</h1>

<table class="table table-hover table-condensed">
  <thead>
    <tr>
      <th>Product</th>
      <th>Phone</th>
      <th>Website</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @cosmetics.each do |cosmetic| %>
      <tr>
        <td><%= cosmetic.Product %></td>
        <td><%= cosmetic.phone %></td>
        <td><%= cosmetic.website %></td>
        <td><%= link_to 'Show', cosmetic, class: "btn btn-link" %></td>
        <td><%= link_to 'Edit', edit_cosmetic_path(cosmetic), class: "btn btn-link" %></td>
        <td><%= link_to 'Destroy', cosmetic, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-link" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Cosmetic', new_cosmetic_path %>
